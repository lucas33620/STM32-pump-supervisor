# Common

Shared project resources used across multiple layers.

This layer contains common and reusable project elements such as:
- shared types
- global configuration
- utility helpers
- platform-independent definitions

Responsibilities:
- centralize common definitions
- reduce duplication across layers
- provide stable project-wide shared elements

Rules:
- should remain lightweight
- should not depend on upper layers
- should avoid becoming a miscellaneous dump folder